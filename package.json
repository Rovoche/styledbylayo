{
  "name": "styledbylayo-static",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "lint": "echo 'No linting required for static sites.'"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
